<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodRecipe"
        tools:targetApi="tiramisu">

        <!--            android:name=".feature.ui.splash.SplashActivity"-->
        <activity
            android:name=".feature.presentation.authentication.register.RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity
            android:name=".feature.presentation.authentication.createaccount.CreateAccountActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.presentation.authentication.createinfo.CreateInfoActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.presentation.authentication.forgot.ForgotPasswordActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.presentation.authentication.login.LoginActivity"
            android:screenOrientation="portrait" />

<!--        <activity-->
<!--            android:name=".feature.presentation.authentication.register.ActivityRegister"-->
<!--            android:screenOrientation="portrait" />-->

        <activity
            android:name=".feature.presentation.dishprefered.ActivityDishPreferred"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.presentation.main.MainActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>